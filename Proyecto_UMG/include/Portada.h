#ifndef PORTADA_H
#define PORTADA_H

#include <string>
using namespace std;

class Portada {
public:
    // Método para dibujar la portada
    static void dibujarPortada(const string& nombreArchivo);
};

#endif // PORTADA_H
